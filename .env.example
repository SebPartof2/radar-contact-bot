# Discord application — the only required configuration.
# Channel, manager role, and monitors are set per server with /rc setup.
DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# Optional
POLL_INTERVAL_MS=15000
MISSED_POLLS_BEFORE_OFFLINE=2
TZ=UTC
